pydantic>=2.5,<3
pyyaml>=6.0,<7
click>=8.1,<9
rich>=13.0,<14
python-dateutil>=2.8,<3

# Testing
pytest-asyncio>=0.21.0     # Async test support for LLM extractor tests

# Phase 2: LLM extraction layer (all optional — only install what you use)
anthropic>=0.40.0          # Claude provider
openai>=1.50.0             # OpenAI provider
httpx>=0.27.0              # Ollama HTTP client
tiktoken>=0.7.0            # Token counting for cost estimation

# Phase 4: RAG server
fastapi>=0.100.0           # REST API framework
uvicorn>=0.24.0            # ASGI server

# Phase 4: vector search (optional — pure BM25 works without these)
# sentence-transformers>=2.2.0   # dense embeddings (all-MiniLM-L6-v2)
# chromadb>=0.4.0                # vector store persistence
